import 'package:flutter/material.dart';

class BusDriverHomeScreen extends StatelessWidget {
  const BusDriverHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Bus driver home'),
    );
  }
}